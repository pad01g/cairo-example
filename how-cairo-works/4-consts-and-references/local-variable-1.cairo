
func main() {
    // firstly, get local variable space
    // alloc_locals;
    ap += (SIZEOF_LOCALS);

    // then, put temp variable on top of that
    tempvar x = 0;

    local y;

    // you can assign to local later
    y = 6;
    ret;
}
